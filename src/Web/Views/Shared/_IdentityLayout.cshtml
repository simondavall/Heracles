@{
    Layout = "/Views/Shared/_Layout.cshtml";
}

<div class="identityFeatures">

    @RenderBody()

</div>

@if (IsSectionDefined("Scripts"))
{
    DefineSection("Scripts", () => RenderSectionAsync("Scripts", false));
}
